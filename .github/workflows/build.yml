name: Build Executable

on:
  push:
    branches: [ gui-version ]
  pull_request:
    branches: [ gui-version ]

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller
        pip install ttkbootstrap pillow tensorflow numpy==1.26.0 tensorflow==2.18.0 scipy tqdm essentia

    - name: Build with PyInstaller
      run: >
        pyinstaller --noconfirm --onefile --windowed chartgen_gui.py
        --hidden-import=ttkbootstrap
        --hidden-import=ttkbootstrap.themes
        --hidden-import=PIL._tkinter_finder


    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: chartgen-${{ matrix.os }}
        path: dist/chartgen_gui*